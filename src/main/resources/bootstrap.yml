spring:
  application:
    name: client-msf-dev
  cloud:
    config:
      uri: http://localhost:8888
  kafka:
    bootstrapServers: http://localhost:9092
    properties:
      fetch:
        max:
          wait:
            ms: 60000